# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET		/booking/:bookingId					controllers.Booking.bookings(bookingId:String)

POST	/commit						controllers.Booking.commit


